modmap:
  - name: fucking_keyboard
    remap:
      super_l: alt_l
      super_r: alt_r

      alt_l: super_l
      #held: [super_l]
      #  alone: [pageup]
      #  alone_timeout_millis: 1000

      alt_r: alt_r
        #held: [alt_r]
        #alone: [pagedown]
        #alone_timeout_millis: 1000

        #pagedown:
        #skip_key_event: false
        #press: { launch: ["hyprctl", "switchxkblayout", "xremap", "0"] }
        #release: { launch: [] }
        
        #pageup:
        #skip_key_event: false
        #press: { launch: ["hyprctl", "switchxkblayout", "xremap", "1"] }
        #release: { launch: [] }
        #
    device:
      not: ["AT Translated Set 2 keyboard"]
